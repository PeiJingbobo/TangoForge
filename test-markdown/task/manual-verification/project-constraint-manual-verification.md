# 项目约束：任务人工验证手册规则

## 1. 元数据

- 任务标识：project-constraint-manual-verification
- 任务标题：要求每个任务交付人工验证手册
- 关联约束：`AGENTS.md` Agent workflow、Verification、Definition of done
- 手册状态：待执行
- 最近更新日期：2026-08-01

## 2. 验证目标

确认仓库级约束、任务流程和模板一致要求每个任务产出独立人工验证手册，并且未执行的人工验证
不会被误报为完成。

## 3. 前置条件

- 任意能够查看 Markdown 的环境。
- 不需要启动应用、连接 Mac 或使用 Windows 测试主机。
- 不需要任何真实端点、账号、凭据或证书。

## 4. 验证步骤

| 编号 | 操作 | 预期结果 | 状态 |
|---|---|---|---|
| 1 | 打开根目录 `AGENTS.md`，查看 Agent workflow | 明确要求每个任务在 `docs/task/manual-verification/` 创建或更新独立手册 | 未执行 |
| 2 | 查看 `AGENTS.md` 的 Verification 和 Definition of done | 交付报告必须给出手册路径；手册缺失时任务不满足完成条件 | 未执行 |
| 3 | 打开 `docs/task/README.md` 的导航和使用方式 | 可进入手册规则与模板；必需人工验证未执行时保持“待人工验收” | 未执行 |
| 4 | 打开 `docs/task/task-template.md` | 元数据、检查清单和完成条件都包含任务级手册要求 | 未执行 |
| 5 | 复制 `manual-verification/template.md` 做一次不保存的演练 | 模板包含前置条件、步骤、预期、证据、失败路径、清理、结果和不适用说明 | 未执行 |
| 6 | 检查“不适用”规则 | 环境不可用只能标记“未执行”，不能借此标记“不适用” | 未执行 |

## 5. 失败与恢复

- 若任一入口缺少手册要求，任务流程存在不一致，应修正文档后重新执行全部步骤。
- 若导航链接打不开，检查相对路径和文件名大小写。
- 本验证只读，不应产生需要清理的文件或配置。

## 6. 本次执行记录

- 执行日期：未执行
- 执行人：未记录
- 结果：未执行
- 自动化文档检查：已通过；8 个约束/流程文件存在，任务导航链接缺失 0，敏感模式命中 0
- 尚未验证项：全部人工步骤
- 残余风险：未来任务若不遵循根目录约束，仍可能漏建手册；交付复核必须检查手册路径。
