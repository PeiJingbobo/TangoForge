# TF-055 PERT 任务图重构 — 任务日志

## 2026-08-18

- 进展：根据参考 PERT 图与当前 47 节点截图，确认矩形节点、端口相位和跨层 S 曲线是关系混乱的主要来源。
- 任务同步：TangoForge 实时任务板未登记 TF-055，本次沿用现有 TF-028，状态由 done 回退 doing，并记录返工范围。
- 决策：不新增 React Flow/ELK 依赖；保留现有 D3 zoom，用纯 TypeScript 实现最长路径分层、barycenter 排序、行间走廊路由和路径追踪。
- 实现：圆形状态节点；同源边共享右侧锚点与短主干；16px 透明边命中层；选边后上游/下游高亮；固定视口无限平移缩放；真实 bounds 适应屏幕。
- UI 规范：工具栏复用已安装的 shadcn `Input`、`Button` 和项目语义色；图本体使用专用 SVG。
- 踩坑：UNC 补丁过程中 `pert-view.tsx` 尾部遗留无效单字节并触发 SMB inode 租约；字节级确认后截断旧辅助函数、校验 UTF-8，并通过同目录副本在 Mac 原子替换 inode。
- 验证：新增 18 个 PERT 定向测试全绿；完整前端 48 文件/303 测试全绿；typecheck、ESLint、Prettier、production build 全绿。
- 未验证：SSH 图形会话执行 `screencapture` 返回 `could not create image from display`，因此真实 GUI 视觉、拖拽手感和复杂路径可读性等待人工核验。

### 人工反馈返工

- 反馈：正交折线、边焦点矩形、固定屏幕尺寸箭头、节点状态角点、机械对齐和单色细线均不符合目标。
- 实现：路径改为共享起点/初始切线的三次贝塞尔；节点按任务 ID 确定性横纵错位；根任务使用黄金角色相，后代出边继承谱系色；默认/相关/选中线宽提升至 2.5/3.5/4.5px。
- 缩放：删除可见边 `non-scaling-stroke`，marker 改用 `userSpaceOnUse`；箭头和线宽随 D3 画布 transform 同步缩放。
- 交互：透明命中边显式关闭 focus outline，保留鼠标与键盘可操作性；移除节点状态角点。
- 验证：PERT 定向测试 21/21（含可避免交叉的换序断言）；完整前端 48 文件/306 测试通过；typecheck、lint、format:check、production build 通过。
- 未验证：复杂真实依赖图的最终交叉密度、颜色可辨性、缩放手感仍需人工按新版手册核验。

### 第二轮人工反馈返工

- 反馈：扩大画布后仍有连接线穿过节点；节点间距不足；选中路径时无关路径的箭头仍保持高亮。
- 根因：单段贝塞尔只依据源/目标端点，没有中间节点障碍模型；SVG marker 的 path 填充不继承引用路径的 `strokeOpacity`。
- 实现：层间距提升至 260px、行间距提升至 150px；为非端点节点增加 20px 排斥区，以每段 72 点采样检测碰撞；碰撞边和跨多层长边改走三段贝塞尔安全走廊。
- 箭头：每个根谱系生成 normal/active/dimmed 三套 marker，箭头透明度与线条状态同步为 62%/100%/8%。
- 验证：PERT 定向 23/23；typecheck、lint、format:check、production build 通过；完整前端单独复跑 48 文件/308 测试通过。
- 待核验：真实 47 节点图的安全走廊密度、长边交叉观感和无关箭头降噪效果仍需人工确认。

### 第三轮人工反馈返工

- 反馈：末端 TF-051 只有 TF-050 一个依赖，但被放置在末层下方，连接线纵向跨越多个节点并产生交叉。
- 根因：旧算法只优化离散层内顺序，坐标仍按每层节点数独立居中；目标层的 TF-030 占位把 TF-051 推离唯一父节点。
- 实现：增加 18 轮父子实际 y 中位线松弛；每轮允许层内按目标 y 重排，并用 PAVA 单调回归求满足 214px 最小间距的最小误差坐标。
- 真实数据：直接读取 `/api/graph` 计算，TF-050 → TF-051 的纵向差从旧布局约 854px 收敛为 0px，边为相邻层单段贝塞尔。
- 验证：PERT 定向 25/25；完整前端 48 文件/310 测试；typecheck、lint、format:check、production build 全部通过。
- 待核验：真实 Electron 画布中末层整体分布和其他低度末端节点仍需人工视觉确认。

### 第四轮人工反馈返工

- 反馈：搜索节点虽已高亮，但全图其他谱系连接线仍保持正常对比度；搜索完成后视口没有自动移动到结果区域。
- 根因：搜索状态只参与节点透明度判断，没有生成相关依赖边集合，也没有触发 D3 zoom transform。
- 实现：新增节点级路径追踪，对每个搜索结果分别遍历上游与下游并合并实际经过的边；搜索输入会清除旧边选择，无关边及箭头统一降至 8%。
- 视口：以全部命中节点的联合包围盒计算缩放与中心点，预留节点标题空间；单结果最大 1.65x，多结果确保全部落入可视区域。
- 验证：PERT 定向 29/29；完整前端门禁结果见任务总结。
- 待核验：真实项目搜索单个/多个任务时的视觉聚焦范围与缩放手感仍需人工确认。

### 第五轮人工反馈返工

- 反馈：搜索结果缺少持续可辨认的节点标记；点击背景后应取消搜索路径高亮，但不能丢失结果定位。
- 状态拆分：搜索匹配集合负责结果标记，`searchHighlightActive` 仅控制路径 active/dimmed 与非命中节点降噪，两者不再绑定。
- 实现：命中节点外侧增加半径 `node.radius + 11px` 的主色同心圆；2.5px 描边与节点主体保留约 10px 间隙。
- 交互：点击 SVG 背景同时清除选边和搜索高亮状态，全图恢复普通对比度；搜索输入及结果同心圆保持不变。输入新查询后自动重新启用路径高亮。
- 验证：PERT 定向 30/30；完整前端门禁结果见任务总结。
- 待核验：真实缩放档位下同心圆间隙、颜色对比和背景点击手感仍需人工确认。

### 第六轮人工反馈返工

- 反馈：多条跨层路径仍会选择相同的外侧安全走廊并完全重合；长依赖应能绕过内部节点和连接线，并以不同曲率接回目标。
- 根因：旧路由器只对节点独立避障，不记录已完成路径；每条边用同一组候选和“距离中线最近”规则，因而会重复选择同一个 corridorY。
- 实现：改为短跨度优先、长跨度后置的顺序布线；为直接曲线、两段宽缓弧线、三段平滑走廊生成多组内部/外部曲率通道。
- 边避让：将已完成路径以 18px 采样写入空间哈希，候选评分新增近距离重叠惩罚；共享源主干和目标附近保留汇聚豁免，其余路径自动分道。
- 平滑性：两段弧线和三段走廊的每个连接点都保持两侧控制向量共线；目标侧控制柄缩短，减少多入边提前合并。
- 画布：允许上侧外部通道临时使用负 y，路由完成后整体归一化下移；下侧通道可扩展逻辑画布，不再强迫长边挤入内部。
- 真实数据：47 节点/134 边，5 次布局平均约 527ms，非端点节点碰撞 0，18px 采样严重重叠路径对 0，画布约 3248×3442px。
- 验证：PERT 定向 32/32；完整前端门禁结果见任务总结。
- 待核验：真实 Electron 中外侧长弧线的视觉曲率、画布缩小后的辨识度以及极端高入度节点附近的汇聚观感。

### 第七轮人工反馈返工

- 反馈：逻辑图虽然扩张，但首次“适应全图”仍把节点压成缩略图；节点相距过近，部分连接仍呈多段走廊观感，不够平滑。
- 根因：首次加载与显式“适应屏幕”共用同一行为；避让评分对轻微路径接近惩罚过重，路由器频繁放弃单段贝塞尔并选择远端多段通道；边界又错误包含不可见控制点。
- 无限视口：画布高度改为随窗口扩展且最小 620px；平移不设边界，缩放范围扩至 0.04x–3x；首次加载保持至少 0.58x 节点可读尺度并以节点范围居中，只有点击“适应屏幕”才进入全图概览。
- 点阵：背景 pattern 与图形使用同一 D3 zoom transform，拖拽和缩放后仍表现为连续的无限画布坐标系。
- 稀疏布局：层间净距增至 420px、行间净距增至 260px、画布边距增至 180px，圆心节距达到 484×324px。
- 平滑路由：新增反解曲线中点的单段贝塞尔候选并给予优先级；降低轻微接近惩罚，保留节点碰撞硬约束，两段/三段走廊只作持续重合和避障兜底。
- 边界修正：使用实际曲线采样点计算逻辑 bounds，控制点只随整体位移，不再制造虚假空白。
- 真实数据：47 节点/134 边，布局约 499ms；122 条单段、8 条两段、4 条三段；节点碰撞 0、严重重叠 0；逻辑内容约 4796×5064px。
- 验证：PERT 定向 33/33；完整前端门禁结果见任务总结。
- 待核验：真实 Electron 首次进入的局部尺度、无限拖拽手感、全图概览和长弧线视觉平滑度。

### 第八轮人工反馈返工

- 反馈：继续扩大间距后仍存在密集交叉和不够平滑的长路径；用户明确要求连接线质量优先于节点对齐，并要求调研成熟排列算法。
- 调研：比较 Graphviz dot、ELK Layered 与 yFiles hierarchical 的 proper-layer、交叉最小化、节点放置和边路由流程；使用真实 47 节点/134 依赖数据验证自研、ELK 与 Graphviz 方案。
- 约束确认：真实依赖图经 NetworkX 平面性检测为非平面图；保留每条依赖独立可选时，二维绝对零边交叉不可实现。将“连接线不穿节点”设为硬约束，边交叉设为可量化最小化目标。
- 实现：新增 `pert-graphviz-layout.ts`，按需加载 `@viz-js/viz`；Graphviz dot 以 LR 分层、长边虚拟节点、32 倍 mincross、重新交叉优化和分量打包生成坐标。
- 路由：使用 `splines=spline`，直接把 Graphviz `b` 操作解析成 SVG 三次贝塞尔；固定圆形障碍和 east/west 端口保证边绕开节点、同源边共享初始端点，连接线没有折线路径。
- 间距：层间净距按 520px 配置，层内虚拟节点间距 1.6in；非连通分量间距 128pt，稀疏图保留在无边界画布中，不再压进固定视口。
- 降级：Graphviz WASM 异步加载失败时保留同步 TypeScript Sugiyama 结果，避免全景图空白；实测效果更差的 ELK 实验与依赖已清理。
- 真实数据：Graphviz 布局约 147ms；非端点圆形节点穿越数 0；独立路径交叉对由同步兜底 616 降为 424，减少约 31%。
- 验证：PERT 定向 36/36；完整前端 49 文件/321 测试；typecheck、lint、format:check、production build 全部通过。
- 待核验：真实 Electron 中 Graphviz 大图的首次异步切换、全图观感、长边曲率和选择路径后的可追踪性。

### 第九轮人工反馈返工

- 反馈：Graphviz 原始 B 样条仍带有分段转折感，节点左右距离不足；连接线平滑度优先于节点是否对齐。
- 布局：层间净距由 520px 增至 720px，层内行距由 380px 增至 460px；节点增加最多 90px 的确定性横向扰动，保留分层方向但弱化机械网格。
- 曲线重构：不再直接输出 Graphviz 控制点。先高密度采样 Graphviz 安全路径，重定向共享源端点与目标箭头间隙，再按多级容差简化为导向点。
- 平滑拟合：使用弦长参数化的自然三次样条求解控制点，源端固定水平切线与共享 112px 主干，目标端固定水平切线；相邻曲线段保持一阶切线连续，消除折角和短小锯齿。
- 安全降级：从宽松到精细逐级尝试简化容差，每次拟合都重新执行圆形节点碰撞检测；只有零穿越候选才被采用，否则回退到重定向后的 Graphviz 安全曲线。
- 画布边界：宽高同时纳入重新拟合后的曲线采样点，扩大间距和外侧弧线不会被裁切。
- 验证：PERT 布局定向 24/24；其中新增多段曲线切线连续性与更大画布宽度断言。完整前端门禁结果见任务总结。
- 待核验：真实 Electron 中长边弧度、节点横向错落感及高密度区域的总体视觉可追踪性。

### 第十轮人工反馈返工

- 反馈：画布空白区域应保持抓手，但鼠标进入可选连接线时需要显示普通箭头。
- 实现：保留 SVG 画布 `cursor-grab` / 拖拽时 `cursor-grabbing`；仅在 16px 透明连接线命中层设置 `cursor-default`，可见线仍不接管指针事件。
- 验证：增加组件回归测试，同时断言画布抓手与连接线普通箭头样式。
