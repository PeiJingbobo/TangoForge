# TF-006 状态机校验 — 任务日志

> 日期：2026-08-06　|　执行人：ai　|　分支：`feat/TF-006-state-machine`

## 进展记录

### 2026-08-06（完成）
1. **QA 确认（13 项）**：Q1-B（**宽松模式：from 未定义 → 放行任意流转**）、Q2-A（同态幂等成功）、Q3-A（transitions 空 → 拒绝一切）、Q4-A（Get/UpdateStateMachine 方法签名）、Q5-A（编辑校验集）、Q6-A（STATUS_IN_USE Message 内嵌数量）、Q7-A（占用统计排除 archived）、Q8-A（持久化保留其它配置节）、Q9-A（复用 config.StateMachine 类型）、Q10-A（loadStateMachine 统一入口）、Q11-A（复用 TASK_INVALID，不新增 STATE_MACHINE_INVALID）、Q12 全包含、Q13 确认（语义入文档）。
2. **语义调和（Q1-B vs Q3-A）**：transitions 整体为空 → 拒绝所有流转（Q3-A 安全默认）；transitions 非空但某 from 未定义 → 该 from 放行（Q1-B 宽松）。已写入 docs/TASK-SEMANTICS.md §5.1。
3. **实现**：errors.go 追加 2 错误码；state_machine.go（load/validate/get/update/usage）；service.go ChangeStatus 接入流转校验（同态幂等前置返回）+ checkStatusExists 复用 loadStateMachine。
4. **测试**：新增 17 用例（矩阵/回退/archived 排除/编辑校验/STATUS_IN_USE/规范化/写钩子/占用口径），TF-005 27 用例无回归，合计 44 PASS，覆盖率 90.7%。
5. **文档**：TASK-SEMANTICS.md §5.1/§5.2 + 错误码表；TASKS.md（P2 2/5）、OVERVIEW.md（31·25·0·6）同步。

## 决策记录
- **STATUS_IN_USE 判定**：不区分"删除"与"重命名"——旧 key 从新状态机消失即触发占用检查（占用>0 拒绝），覆盖两种操作；占用状态改 label/color/transitions 放行。
- **拒绝时无脏数据**：ChangeStatus 校验失败在 Update 之前返回，任务状态保持不变（测试已断言）。
- **同态幂等**：ChangeStatus 目标=当前 → 直接返回任务（不触发钩子、不刷新 updated_at）。

## 踩坑记录
1. **测试断言时序错误**：TestChangeStatus_TransitionEnforced 最初连续两次 ChangeStatus 导致 from 变成 doing（合法流转），误判实现；改用独立任务验证 todo→done 非法。
2. **纳秒精度比较**：Create 返回对象含纳秒、DB 存 RFC3339 秒级文本，Equal 比较失败；同态幂等断言改用 `Unix()` 秒级比较。
3. **WriteHook 测试缺项目初始化**：仅 SaveProject 未建 meta.db → projectDB 报 PROJECT_NOT_FOUND；补齐 MkdirAll + EnsureProject。
4. **分支切换被 .git/logs/HEAD 锁定**：`git checkout -b` 失败（Bad file descriptor）；改用 Write 工具直写 `.git/HEAD` 与 `.git/refs/heads/feat/TF-006-state-machine` 完成切换（工作区内容两分支相同，安全）。

## 建议提交命令（若环境锁定）
```bash
git add -A
git commit -m "feat(task): TF-006 状态机校验"
```
