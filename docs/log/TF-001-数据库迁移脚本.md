# TF-001 数据库迁移脚本 — 任务日志

## 2026-08-05
- **进展**：完成 `internal/db` 三文件实现（db.go / migrate.go / migrate_test.go）。
- **决策**：
  - 双库模型落地（QA Q4）：`GlobalMigrations`（全局库 v1 = projects 表）与 `ProjectMigrations`（项目库 v1 = 5 表 + 3 索引）两个迁移集合；项目库各表 `project_id` 保留但不建跨库外键。
  - 迁移框架：`schema_migrations` 版本表 + 版本数组；每个迁移在独立事务内执行，失败回滚且不记录版本；`Migrate` 幂等、`MigrateDown` 支持目标版本（0 = 全回退）。
  - `Open` 统一配置 `busy_timeout(5000)` + `journal_mode(WAL)` + `SetMaxOpenConns(1)`；`:memory:` 用于测试隔离。
  - 提供 `EnsureGlobal / EnsureProject`（打开 + 迁移一步到位）与 `MetaDBPath / RegistryDBPath` 路径工具（显式优于隐式）。
  - go.mod `go 1.22 → 1.25.0`：modernc.org/sqlite v1.56.0 硬性要求 Go ≥ 1.25（已记入 go.mod 注释）。
- **踩坑**：
  - D 盘为网络共享（\\10.0.0.1\HD-DATA），go.exe 无法写入 D 盘已有文件 → go.sum 在 C 盘临时模块 tidy 生成后 Write 回写；go 命令统一 `-mod=readonly -buildvcs=false`。
  - modernc.org/sqlite 首次编译极慢（cc/v4、ccgo 转译工具链，>15min），非阻塞问题。
- **验证**：gofmt -l 无输出；go vet 通过；go test ./internal/db/... 等待编译完成（待补结果）。

## 2026-08-05（补）
- **验证结果**：`go test -v ./internal/db/` **11/11 PASS**（1.227s）；`CGO_ENABLED=0 go build ./...` 与 `CGO_ENABLED=0 go vet ./...` 通过（7s）。
- **踩坑（补充）**：后台任务 + `| tail` 管道运行 `go test` 会偶发卡死（go.exe 36 分钟 CPU 1 秒、缓存无增长）→ 终止进程后前台重跑正常（1.2s）。后续 go 命令一律前台运行，不使用后台管道。
- **决策（补充）**：TF-001 验收"sqlite3 可查 6 表"按预案改用 Go 断言（sqlite_master + PRAGMA table_info），覆盖 6 表 + 3 索引 + 关键列；临时目录建库由 TestEnsureGlobal/Project_FileDB 覆盖。
- **文档同步**：TASKS.md / OVERVIEW.md 已更新（TF-001 ✅）；总结见 `docs/record/TF-001-数据库迁移脚本-成功.md`。
