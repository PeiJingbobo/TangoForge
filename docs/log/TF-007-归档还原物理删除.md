# TF-007 归档 / 还原 / 物理删除 — 任务日志

> 日期：2026-08-06　|　执行人：ai　|　分支：`feat/TF-007-archive`

## 进展记录

### 2026-08-06（完成）
1. **QA 确认（13 项）**：Q1-A（ArchiveResult{Task, DependentCount}）、Q2-B（**幂等归档**，改推荐 A）、Q3（**UI 提前确认流 + 级联置空事务原子**，ArchiveResult 增加 ChildrenCleared）、Q4-A（task.archived）、Q5（**UI 还原前状态检查，FallbackTodo 选项**）、Q6-A（仅 archived 还原、archived_from 缺失回退 todo）、Q7 确认（还原恢复并清空 archived_from）、Q8（**UI 物理删除前子任务检测确认流**，业务层仅级联置空）、Q9-A（DELETE_NOT_ALLOWED + 返回快照）、Q10-A（task.deleted）、Q11-A（repo.Delete + ClearParentsByParentID）、Q12 全包含、Q13 确认。
2. **语义入文档**：docs/TASK-SEMANTICS.md 新增 §8 删除语义（8.1 归档 / 8.2 还原 / 8.3 物理删除 + UI 确认流），错误码表加 DELETE_NOT_ALLOWED，章节重排（原 §8/§9/§10 → §9/§10/§11）。
3. **实现**：repo.go dbtx 事务化改造（*sql.DB/*sql.Tx 共用同一 SQL 实现）；archive.go 三方法；service.go 接口扩展。
4. **测试**：新增 17 用例，合计 61 PASS，无回归；覆盖率 88.8%（错误分支缺口登记 TF-009）。
5. **文档**：TASKS.md（P2 3/5）、OVERVIEW.md（31·24·0·7）同步。

## 决策记录
- **Q2-B 幂等**：归档已归档任务 → 直接返回当前状态（不重复写 archived_from、不触发钩子）——避免 UI 重复点击产生重复审计。
- **Q8-A 解读**："物理删除子任务严格禁止" = **级联层面禁止**（删除父时不提供级联删除子任务能力，仅置空）；回收站中子任务自身可单独物理删除。
- **UI 确认流分层**：业务层提供判断依据（ChildrenCleared、FallbackTodo、STATUS_NOT_FOUND），确认交互归前端 P5（TF-025/026）。

## 踩坑记录
1. **repo 事务化**：原 sqlRepo 绑定 *sql.DB，事务内无法复用 → 引入 dbtx 接口（*sql.DB/*sql.Tx 均满足），newSQLRepo / newSQLRepoTx 双构造，业务层事务边界保持（分层铁律）。
2. **覆盖率 profile 写入被环境拦**：`-coverprofile` 写工作区被拒（Access denied），改用函数级分析不可用；直接判断缺口为 DB 错误分支，登记 TF-009。
3. **钩子序列断言**：幂等测试误把 Create 的 task.created 计入，改为按 action 计数。
4. **级联测试构造**：归档父时子已置空，验证"物理删除父时级联"需先重新挂载子到已归档父（TF-005 允许挂载 archived 父）再删除。
5. **分支切换**：同 TF-006，`.git/logs/HEAD` 锁定时用 Write 工具直写 `.git/HEAD` + `.git/refs/heads/feat/TF-007-archive`。

## 建议提交命令（若环境锁定）
```bash
git add -A
git commit -m "feat(task): TF-007 归档/还原/物理删除"
```
