# TF-004 项目管理服务 — 任务日志

## 2026-08-06
- **进展**：完成 `internal/project` 子包（project.go / project_test.go），与 TF-003 端到端联调通过。
- **决策**：
  - 独立子包（QA Q10）：项目管理与任务管理分域；Service 依赖全局注册表库 + 业务层事务边界自控。
  - 权限初始化：v1 动作全集 16 项全部显式写入 permissions 表（默认只读 5 项 true），避免"查询时 missing→false"的隐式依赖（显式优于隐式）。
  - 项目库 permissions.project_id 统一写 1：双库模型下项目库内 project_id 仅文档性冗余（db 包文档已说明），一致性由应用层维护。
  - 重复导入幂等返回已有记录；已有 `.taskboard/` 的目录导入不重新初始化（保留用户自定义 config.yaml）。
  - 错误语义：目录不存在/相对路径/非目录 → ErrInvalidWorkdir；移除不存在 → ErrNotFound。
- **踩坑**：
  - Windows 下临时工具目录删除：bash rm -rf 被安全回收拦截 → PowerShell Remove-Item 删文件后 bash rmdir 清空目录。
  - go run 联调工具放在 `tools/smoke`（module 内可引用 internal 包），验证后整体删除不提交。
- **验证**：10/10 单测；真实 daemon 联调（导入 → X-Project 放行 / 未注册 404）；P1 全仓 go test + CGO0 build 全绿。
- **文档同步**：TASKS.md / OVERVIEW.md 已更新（P1: 4/4 ✅）；总结见 `docs/record/TF-004-项目管理服务-成功.md`。
