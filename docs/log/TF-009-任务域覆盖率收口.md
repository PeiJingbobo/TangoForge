# TF-009 任务域覆盖率收口 — 任务日志

> 日期：2026-08-06　|　执行人：ai　|　分支：`feat/TF-008-dependency`（与 TF-008 同分支收尾）

## 进展记录

### 2026-08-06（完成）
1. 覆盖率基线：TF-008 前 88.8% → 依赖测试后 89.1% → 错误注入补测后 **92.1%**。
2. `scripts/check_coverage.sh`（THRESHOLD=90）与 `CGO_ENABLED=0 go test -cover ./internal/task/...` 均通过（92.1%）。
3. P2 关闭动作：TASKS.md / OVERVIEW.md（M2 日期 2026-08-06）/ MASTER-PLAN §7 质量门禁打勾。

## 决策记录
- **错误注入三件套（Q9-A，零新依赖）**：
  - meta.db 建成目录 → SQLite Open 失败 → 覆盖 10 个 Service 方法 projectDB 错误分支；
  - `conn.Close()` 后操作 → sql.ErrClosed → 覆盖 TaskRepo 6 方法错误分支；
  - config.yaml tab 缩进 → yaml 解析失败 → 覆盖 loadStateMachine 错误分支。
- 未覆盖的残余：扫描函数的时间/JSON 解析错误分支（需要构造脏 DB 数据，收益低，暂不注入）。

## 踩坑记录
1. **覆盖率 profile 文件写入被环境拦截**（Access denied）——无法用 `go tool cover -func` 定位，靠代码审查判断缺口。
2. **yaml 宽松解析**：`:::` 内容不报错，改用 tab 缩进（见 TF-008 日志）。

## 建议提交命令（若环境锁定）
```bash
git add -A
git commit -m "feat(task): TF-008 依赖关系与无环校验"
git commit -m "test(task): TF-009 任务域覆盖率收口（92.1%）"
```
