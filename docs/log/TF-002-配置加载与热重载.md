# TF-002 配置加载与热重载 — 任务日志

## 2026-08-05
- **进展**：完成 `internal/config` 四文件（config.go / global.go / project.go / config_test.go）。
- **决策**：
  - 新增依赖 `gopkg.in/yaml.v3 v3.0.1`（Q3 范围外，但 YAML 解析必需；纯 Go）。go.mod/go.sum 仍在 C 盘 modbuild tidy 后回写（keepdeps.go 补 yaml import 才保住依赖）。
  - 热重载监听**目录**而非文件（兼容编辑器原子保存：临时文件 + rename）；事件 150ms 去抖，合并连续写入为一次回调。
  - 热重载失败（解析错误）保留旧配置 + slog.Error，不中断 watcher——避免误改配置导致服务失联。
  - SaveGlobal/SaveProject 权限 0600（含凭据）；MkdirAll 自动建父目录。
  - 项目配置 states 为空时回退默认四态（TF-006 再做完整语义校验，本层不做业务判断）。
- **踩坑**：
  - go mod tidy 会移除未被 import 的依赖 → keepdeps.go 必须先补 `_ "gopkg.in/yaml.v3"` 再 tidy。
- **验证**：13/13 PASS（含热重载：改 port 后回调收到 20001）；CGO_ENABLED=0 build/vet 通过。
- **文档同步**：TASKS.md / OVERVIEW.md 已更新（P1: 2/4）；总结见 `docs/record/TF-002-配置加载与热重载-成功.md`。
