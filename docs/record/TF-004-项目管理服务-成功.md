# TF-004 项目管理服务 — 任务总结
> 结果：成功　|　日期：2026-08-06　|　执行人：ai

## 1. 任务范围
交付项目注册表业务层（独立子包 `internal/project`，QA Q10）：目录导入为项目（含 `.taskboard/` 自动初始化）、项目列表、移除记录（绝不删除磁盘数据）、`last_opened_at` 维护，并与 TF-003 中间件完成端到端联调。

## 2. 交付内容
- **新增文件**：
  - `internal/project/project.go` — Project 模型、Service（Import / List / Remove / Touch）、initProjectDir（meta.db 5 表 + config.yaml 默认状态机 + skills/ + 默认权限）、writeDefaultPermissions（v1 动作全集 16 项显式写入）
  - `internal/project/project_test.go` — 10 个测试用例
- **关键实现点**：
  1. Import 幂等：重复导入返回已有记录；目录无 `.taskboard/` 才初始化（已有元数据目录直接注册不覆盖）
  2. 默认权限：`AllActions`（16 项）全部显式写入 permissions 表，默认只读 5 项 true（task.read/graph.read/skill.read/project.read/permission.read），写权限默认 false
  3. Remove 仅删 projects 行，磁盘 `.taskboard/` 数据完好（重新导入可恢复）
  4. List 排序：`last_opened_at` 倒序（从未打开排最后）；Touch 供 X-Project 命中时更新（QA Q12）
  5. 项目库 permissions.project_id 统一写 1（项目库内 project_id 为文档性冗余，一致性由应用层维护）

## 3. 验证结果
- `go test -v ./internal/project/` → **10/10 PASS**（1.7s）
  - 覆盖：新项目完整初始化（meta.db/config.yaml/skills/权限 16 行 5 true）、已有元数据不覆盖、重复导入幂等、非法目录、列表排序、移除后磁盘完好、移除不存在、Touch 更新、Import 对 db.ProjectExists 可见（与 TF-003 中间件联动）
- 真实 daemon 端到端联调：
  - `go run` 导入临时目录 → `.taskboard/` 生成（config.yaml + meta.db + skills）✅
  - daemon 同一注册表库启动：已注册 X-Project → 放行（`404 page not found` 路由兜底，非 PROJECT_NOT_FOUND）；未注册 → `404 PROJECT_NOT_FOUND` ✅
- P1 全仓验证：`go test ./...` 全绿；`CGO_ENABLED=0 go build ./...` 通过；gofmt 无输出

## 4. 遗留问题与后续
- 项目库 permissions 表缺少"项目级查询"便捷方法（TF-011 权限中间件补充 Allowed/Set 业务接口）。
- X-Project 大小写归一化：当前精确匹配（注册与校验均 filepath.Clean）；Windows 大小写歧义在 TF-011 权限查询时统一处理。
- P1 关闭：里程碑 M1 达成，可进入 P2（TF-005 任务基础 CRUD）。
