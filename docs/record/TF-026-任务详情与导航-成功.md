# TF-026 任务详情与导航视图 — 任务总结

> 结果：成功　|　日期：2026-08-06　|　执行人：ai

## 1. 任务范围
任务详情行内编辑（阅读流）、归档/还原、依赖编辑 + 无环提示、导航三视图。

## 2. 交付内容
- `TaskForm.tsx`：行内编辑（标题/描述 click-to-edit、状态/优先级 Select、负责人、标签 chips、依赖选择）；差异提交；sticky 底部保存（UI-VISION QA-3）；子任务只读展示
- `TaskDetail.tsx`：阅读流布局（左表单 + 右属性栏分割线分组）；归档确认（子任务级联提示）→ navigate 看板；还原；CIRCULAR_DEPENDENCY → toast 无环提示
- `NavViews.tsx`：树形 / 时间线 / 状态分类三视图（Tabs），路由 /project/:id/nav
- `TreeNav.tsx`：折叠/展开、搜索过滤、当前高亮（直接消费后端树结构）
- `constants.ts`：PRIORITY_OPTIONS 独立（react-refresh 规范）

## 3. 验证结果
- `pnpm typecheck` / `pnpm lint` 全绿；`pnpm test` **66 用例全过**（TaskForm 5 + TaskDetail 3 + TreeNav 4 + 其余）
- 踩坑沉淀：Radix Select aria-label 必须传给 Trigger 而非 Root（Root 不渲染 DOM）；jsdom 需 hasPointerCapture / scrollIntoView polyfill（已入 setup.ts 全局）

## 4. 遗留问题与后续
- 无
