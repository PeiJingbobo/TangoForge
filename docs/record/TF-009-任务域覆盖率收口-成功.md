# TF-009 任务域覆盖率收口 — 任务总结
> 结果：成功　|　日期：2026-08-06　|　执行人：ai

## 1. 任务范围
补全 `internal/task` 错误分支测试（覆盖率 ≥ 90%），使 `scripts/check_coverage.sh`（THRESHOLD=90）与 `CGO_ENABLED=0 go test -cover ./internal/task/...` 通过；P2 关闭（M2 里程碑达成）。

## 2. 交付内容
- **新增文件**：
  - `internal/task/coverage_test.go` — 错误注入测试（QA Q9-A 策略，不引入新依赖）：
    1. `TestServiceMethods_ProjectDBError`：meta.db 建成目录（Stat 通过但 SQLite Open 失败）→ 覆盖全部 10 个 Service 方法的 projectDB 错误分支
    2. `TestRepoErrors_ClosedConnection`：关闭连接（sql.ErrClosed）→ 覆盖 TaskRepo 全部 6 个方法错误分支
    3. `TestService_ConfigCorrupt`：config.yaml 非法 YAML（tab 缩进）→ 覆盖 loadStateMachine 解析错误分支（Create/ChangeStatus）
- **修改文件**：`docs/task/TASKS.md`（TF-008/009 ✅ + P2 5/5 + M2 达成标注）、`docs/task/OVERVIEW.md`（统计 31·22·0·9 + P2 泳道 [5/5] + M1/M2 里程碑日期）、`docs/task/MASTER-PLAN.md`（§7 质量门禁 P2 退出检查打勾）

## 3. 验证结果
- `./scripts/check_coverage.sh`（THRESHOLD=90，PKG=./internal/task/...）→ **OK: 覆盖率 92.1% >= 90%**
- `CGO_ENABLED=0 go test -count=1 -cover ./internal/task/...` → **92.1%**（> 90% 门槛）
- 全仓：`gofmt -l` 干净、`go vet ./...` 通过、`CGO_ENABLED=0 go build ./...` 通过、`go test ./...` 全绿（api/config/db/project/task 全部 ok）
- 测试合计：80/80 PASS（TF-005~008 + TF-009 补测）

## 4. 遗留问题与后续
- **P2 已关闭（M2 里程碑达成，2026-08-06）**：`internal/task` 覆盖率 92.1%，质量门禁打勾（MASTER-PLAN §7）。
- 草稿导入流程（import_drafts）的覆盖率将在 TF-018 落地后随其测试补齐（P4 阶段，不属本任务范围——TF-009 验收范围为 P2 已交付代码）。
- git 提交：`.git/logs/HEAD` 环境锁间歇性存在；提交命令见 `docs/log/TF-009-任务域覆盖率收口.md`。
