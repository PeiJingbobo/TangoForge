# TF-006 状态机校验 — 任务总结
> 结果：成功　|　日期：2026-08-06　|　执行人：ai

## 1. 任务范围
交付项目级可配置状态机：从项目 `config.yaml` 的 `state_machine` 节加载（缺失回退默认四态）；ChangeStatus 接入流转校验（`INVALID_TRANSITION`）；状态机编辑 API（Get/UpdateStateMachine，占用状态不可删除/重命名，`STATUS_IN_USE` 携带占用数）。

## 2. 交付内容
- **新增文件**：
  - `internal/task/state_machine.go` — loadStateMachine（统一加载入口）、validateTransition（Q1-B/Q3-A 语义）、GetStateMachine / UpdateStateMachine（编辑校验 + STATUS_IN_USE + 持久化 + 写钩子）、validateStateMachineEdit（key 唯一/非 archived/states≥1/transitions 引用校验/to 去重）、statusUsage（占用统计，Q7-A）
  - `internal/task/state_machine_test.go` — 17 个测试用例
- **修改文件**：
  - `internal/task/errors.go` — 追加 `INVALID_TRANSITION` / `STATUS_IN_USE` 码与哨兵、`NewInvalidTransition` / `NewStatusInUse`
  - `internal/task/service.go` — Service 接口加 Get/UpdateStateMachine；ChangeStatus 接入流转校验 + 同态幂等（Q2-A）；checkStatusExists 统一走 loadStateMachine
  - `docs/TASK-SEMANTICS.md` — 新增 §5.1 流转校验、§5.2 状态机编辑，错误码表与 §10 边界同步
  - `docs/task/TASKS.md`、`docs/task/OVERVIEW.md`
- **关键实现点**：
  1. 流转校验语义（QA Q1-B 宽松 + Q3-A 特例）：transitions 整体为空 → 拒绝一切流转；非空时 from 有规则 → 目标须在 to 列表；from 无规则 → 放行
  2. 同态流转幂等成功（Q2-A）：不校验、不刷新 updated_at
  3. STATUS_IN_USE：占用状态（status=key 且非 archived 的任务数 > 0）不可删除/重命名，Message 携带占用数；占用状态允许改 label/color/transitions
  4. 编辑持久化保留其它配置节（Q8-A）：LoadProject → 替换 state_machine → SaveProject
  5. 写钩子 `state_machine.changed`（TF-014 WS 接入点）

## 3. 验证结果
- `go test -count=1 -v ./internal/task/` → **44/44 PASS**（TF-005 27 + TF-006 新增 17，无回归）
- `internal/task` 覆盖率 **90.7%**（> 90% 门槛）
- 全仓：`CGO_ENABLED=0 go build ./...`、`go vet ./...`、`gofmt -l` 干净、`go test ./...` 全绿

## 4. 遗留问题与后续
- **git 提交**：分支切换与提交受 `.git/logs/HEAD` 间歇性锁定影响（本次分支切换经 refs 直写完成）；提交命令见 `docs/log/TF-006-状态机校验.md`，若环境锁定可 IDE 内手动提交。
- 归档/还原（TF-007）：archived 保留态语义已就绪，归档时 `archived_from` 记录与级联置空在 TF-007 落地。
- depends_on 环校验（TF-008）。
- TF-009 覆盖率收口将在 P2 末尾统一执行。
