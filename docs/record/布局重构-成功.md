# TF-029 布局重构：全局导航 + 项目二级 Tab + 首选项页 — 任务总结

> 结果：成功　|　日期：2026-08-06（深夜，用户驱动的布局改造）　|　执行人：ai

## 1. 需求（用户提出）
1. 增加左侧全局导航栏：一级导航仅公共功能（项目概览/设置/亮暗切换）；顶部=项目概览菜单，中部=项目列表（flex:1 占满可滚动），底部=亮暗切换/设置/守护进程状态点；
2. 全局首选项页：LLM 设置、外观等分组归类；实时保存无确定按钮；保存时校验值有效才真正保存；
3. 看板/导入导出/全景图/任务导航/权限/Skills/审计等以项目为单位，先选项目才可操作；激活项目后右侧顶部出现二级功能 tab。

## 2. 交付内容
### 后端（213a166）
- `config.Validate`：端口/BaseURL(URL 合法)/api_kind 枚举/model 必填/数值校验，中文错误（validate_test 15 例）
- `GET/PUT /api/config`：仅 UI（回环+X-UI-Token）；GET 脱敏（api_key/api_token 掩码 前4+****+后2）；PUT 校验通过才保存（422 CONFIG_INVALID 不落盘）→ SaveGlobal 写盘 + SetConfig 热更新 + audit；api_key 空=保留
- Server 加 configPath；NewServer 签名扩展；路由挂 /api 全局组（豁免 X-Project）
### 前端（0af0c25）
- AppLayout 重构：左侧全局导航（项目概览/项目列表 flex-1 滚动/底部主题+设置+守护进程状态点 5s 轮询）；右侧激活项目后二级 tabs（看板/导航/全景图/导入导出/权限/Skills/审计）
- 路由重构：/ 概览、/settings 首选项、/project/:id/{7 个二级 + tasks}
- WorkspacePage → 项目概览（导入入口+最近项目）；ImportExportPage 合并；Permissions/Skills 移入项目二级；AuditPage 新增（列表+动作过滤）
- SettingsPage 首选项：LLM/外观/守护进程 分组，debounce 500ms 实时保存，校验失败 toast+回滚；外观 localStorage 持久化（useThemeMode）
- daemon:status IPC（纯探活）；useConfig/useDaemonStatus hooks

## 3. 验证结果
- 前端 typecheck/lint 全绿；**89 用例全过**（AppLayout 3 + SettingsPage 3 + WorkspacePage 概览适配 + 其余）
- 后端 go test 全量绿；daemon 重建重启；curl 验证：无 token 403、带 UI token 200 + 脱敏（sk-a****d3）

## 4. 遗留
- 主题持久化已由 useThemeMode 完成（TF-022 遗留项清除）
- P6 剩余任务（TF-030 集成测试、TF-031 交付文档）待用户 mac 实测后推进
