# TF-025 看板视图 — 任务总结

> 结果：成功　|　日期：2026-08-06　|　执行人：ai

## 1. 任务范围
看板视图：状态机动态列、任务卡片、dnd-kit 拖拽流转（乐观更新/回滚）、虚拟滚动、过滤搜索。

## 2. 交付内容
- `components/kanban/task-card.tsx`：优先级色带（红高灰低 5 级）、标签哈希着色（5 组 token 软色，无硬编码）、useSortable
- `components/kanban/kanban-column.tsx`：背景色差分组列 + 列内 @tanstack/react-virtual 虚拟滚动（estimateSize 108 + initialRect 兼容测试）
- `components/kanban/kanban-board.tsx`：DndContext + DragOverlay + 多列 droppable
- `components/kanban/drag-logic.ts`：拖拽目标解析纯函数（5 单测）；`tree-utils.ts`：展平/补丁（3 单测）
- `features/tasks/KanbanView.tsx`：搜索/标签过滤、状态机动态列、快速新建 Dialog、点击卡片跳详情
- `hooks/useKanban.ts`：乐观更新 + INVALID_TRANSITION 回滚 + toast（3 单测）
- KanbanView 4 个 RTL 测试（渲染/搜索/标签过滤/新建）

## 3. 验证结果
- `pnpm typecheck` / `pnpm lint` 全绿；`pnpm test` **54 用例全过**
- 踩坑：dnd-kit PointerSensor 在 jsdom 模拟拖拽需 PointerEvent polyfill（弃用，改为 drag-logic 纯函数 + useKanban hook 单测）；react-virtual 在 jsdom 无布局 → 测试需 ResizeObserver 即时 borderBoxSize 回调 + getBoundingClientRect stub（已沉淀为 KanbanView.test 复用模式）

## 4. 遗留问题与后续
- 5,000 条 mock 数据流畅性：mac 实测项（实现已具备虚拟滚动，TASKS.md 勾选保留待实测）
