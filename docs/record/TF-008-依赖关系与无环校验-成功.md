# TF-008 依赖关系与无环校验 — 任务总结
> 结果：成功　|　日期：2026-08-06　|　执行人：ai

## 1. 任务范围
交付 `depends_on` 依赖关系校验：Create/Update 写入前校验（依赖存在性 + 自依赖 + 多跳环），非法写操作直接拒绝且不产生脏数据。

## 2. 交付内容
- **新增文件**：
  - `internal/task/dependency.go` — `validateDependencies`（存在性 + 自依赖 + 多跳环 DFS，方向语义 A 依赖 B）、`cycleReaches`（DFS 环判定）
  - `internal/task/dependency_test.go` — 9 个测试用例
- **修改文件**：
  - `internal/task/errors.go` — 追加 `DEPENDENCY_NOT_FOUND` / `CIRCULAR_DEPENDENCY` 码与哨兵、`NewDependencyNotFound` / `NewCircularDependency`
  - `internal/task/service.go` — Create（uuid 提前生成用于自依赖检测）、Update（`in.DependsOn` 更新前校验）接入
  - `docs/TASK-SEMANTICS.md` — 新增 §9 依赖语义（章节重排 §9→§12）
- **关键实现点**：
  1. 方向语义（Q1-A）：`A.depends_on=[B]` = A 依赖 B；环 = 沿 depends_on 图走回自身
  2. 校验先于写入（Q4-A）：Create/Update 单语句原子，拒绝不产生脏数据（语义等价事务内完成）
  3. 多跳环（Q5-A）：基于"更新后集合 + 其余任务现有集合"构建依赖图 DFS；自依赖（Q7-A）直接拒绝
  4. 依赖已归档任务允许（Q3-A），与 TF-007"归档被依赖仅提示不阻断"闭环
  5. Create 场景 taskID="" 仅校验存在性（新任务无入边，无多跳环可能）

## 3. 验证结果
- `go test -count=1 -v ./internal/task/` → **80/80 PASS**（新增 9 用例，无回归）
- 全仓：`CGO_ENABLED=0 go build ./...`、`go vet ./...`、`gofmt -l` 干净
- 覆盖率随 TF-009 收口后统一验证（92.1%）

## 4. 遗留问题与后续
- 无（依赖校验语义已完整落地；TF-018 导入草稿流的 depends_on 校验将复用 validateDependencies）。
- TF-009 覆盖率达标后，P2 关闭（见 TF-009 总结）。
