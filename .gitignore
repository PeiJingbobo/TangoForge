# ---- Go 编译产物 ----
bin/
*.exe
*.test
*.out
coverage.html
coverage.out

# ---- 运行时数据（不入库）----
# 项目本地元数据目录（含 meta.db / config.yaml / skills/ / audit.log）
.taskboard/
# WorkBuddy 会话数据（记忆/技能缓存）
.workbuddy/

# ---- Agent 宿主安装产物（TF-042：技能包安装目录 + 技能锁） ----
# 注意：.github/（CI workflows）与 .husky/（hooks 配置）是项目基础设施，不得忽略。
.agents/
.claude/
.cursor/
.kilocode/
skills-lock.json

# ---- 前端依赖与构建产物 ----
node_modules/
app/node_modules/
app/dist/
app/out/
app/release/
release/
*.blockmap
*.tsbuildinfo
app/*.local

# ---- OS / IDE ----
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# ---- 日志 / 临时 ----
*.log
tmp/
*.tmp
_tmp_*

# ---- 文档（TF-045：仓库仅保留源代码，docs 移出 git 管理；磁盘文件保留） ----
docs/

# ---- 环境 ----
.env
.env.local

# ---- 本地测试数据（磁盘保留，不入库） ----
test-markdown/
